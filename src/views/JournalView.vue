<script setup lang="ts">/*
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { getJournalById } from '../services/api'
import type { JournalDetails } from '../types/journal'
import JournalSwiper from '../components/journal/JournalSwiper.vue'

const route = useRoute()
const journal = ref<JournalDetails | null>(null)

onMounted(async () => {
  journal.value = await getJournalById(route.params.id as string)
})*/
</script>

<template>
  <div></div>
</template>
